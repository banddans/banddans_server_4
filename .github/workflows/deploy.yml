name: Deploy to VPS

on:
    push:
        branches:
            - main
    workflow_dispatch:
jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Setup SSH
              run: |
                  mkdir -p ~/.ssh
                  echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/github-actions
                  chmod 600 ~/.ssh/github-actions
                  ssh-keyscan -H banddans.se >> ~/.ssh/known_hosts

            - name: Deploy files
              run: |
                  rsync -avz --delete ./ banddans.se:~/src/banddans_server_4
